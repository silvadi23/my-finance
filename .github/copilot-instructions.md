# Copilot Instructions

## Project Guidelines
- Golden Rule: Clean, readable code. Methods should be single-purpose and self-documenting. Intention must be clear from reading the code itself - use descriptive method names instead of comments. Comments are only for logic details or small hints, never to explain what the code does. Always refactor code blocks with explanatory comments into separate, well-named methods.